[package]
name = "stake-contract"
version = "0.10.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dusk-bls12_381-sign = { version = "0.4", default-features = false, features = ["rkyv-impl"] }
dusk-bytes = "0.1"
rusk-abi = { path = "../../rusk-abi", features = ["wee_alloc"] }
transfer-contract-types = { path = "../transfer-types", default-features = false }
stake-contract-types = { path = "../stake-types", default-features = false }

[dev-dependencies]
piecrust = { git = "https://github.com/dusk-network/piecrust" }
transfer-wrapper = { path = "../../test-utils/transfer-wrapper", default-features = false }
hex = "0.4"
rand = "0.8"
