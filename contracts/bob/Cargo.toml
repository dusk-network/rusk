[package]
name = "bob"
version = "0.3.0"
edition = "2021"
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rusk-abi = { version = "0.11.0", path = "../../rusk-abi", features = ["debug"] }
