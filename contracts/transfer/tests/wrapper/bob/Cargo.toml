[package]
name = "bob"
version = "0.1.0"
edition = "2021"
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dusk-abi = "0.10"
canonical = "0.6"
canonical_derive = "0.6"

rusk-abi = { path = "../../../../../rusk-abi", features = ["module"] }

[workspace]
