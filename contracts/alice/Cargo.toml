[package]
name = "alice"
version = "0.3.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
execution-core = { workspace = true }
rusk-abi = { workspace = true, features = ["abi", "dlmalloc"] }
