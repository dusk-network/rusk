[package]
name = "bid-contract"
version = "0.1.0"
authors = ["Jules de Smit <jules@dusk.network>"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
canonical = "0.1.0"
canonical_derive = "0.1.0"
canonical_host = { version = "0.1.0", optional = true }
dusk-blindbid = {git = "https://github.com/dusk-network/dusk-blindbid", tag = "v0.3.0"}

[features]
host = ["canonical_host"]
