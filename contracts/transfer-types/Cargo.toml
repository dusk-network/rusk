[package]
name = "transfer-contract-types"
version = "0.1.0"
edition = "2021"

[dependencies]
dusk-plonk = { version = "0.13", default-features = false, features = ["rkyv-impl", "alloc"] }
dusk-pki = { version = "0.11", default-features = false, features = ["rkyv-impl"] }
dusk-bls12_381 = { version = "0.11", default-features = false, features = ["rkyv-impl"] }
dusk-bytes = "0.1"
phoenix-core = { version = "0.18", default-features = false, features = ["rkyv-impl"] }
dusk-poseidon = { version = "0.28", default-features = false, features = ["alloc"] }
nstack = { version = "0.16", default-features = false }
rusk-abi = { path = "../../rusk-abi" }

rkyv = { version = "0.7", default-features = false, features = ["alloc"] }
bytecheck = { version = "0.6", default-features = false }

[dev-dependencies]
transfer-circuits = { path = "../../circuits/transfer" }
rusk-profile = { path = "../../rusk-profile" }
dusk-jubjub = { version = "0.12", default-features = false }
hex = { version = "0.4", default-features = false }
rand = { version = "0.8", default-features = false, features = ["std_rng"] }

[features]
default = ["host"]
host = ["rusk-abi/host"]
